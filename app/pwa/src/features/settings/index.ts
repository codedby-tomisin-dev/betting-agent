// Public API for settings feature
export { SettingsContainer } from './components/SettingsContainer';

// Re-export types that consumers might need
export type { BettingSettings, RiskLevel } from './types';
